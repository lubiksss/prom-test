version: '3'

services:
  app:
    container_name: app
    build: .
    ports:
      - 2112:2112

  prometheus:
    image: prom/prometheus
    ports: 
      - 9090:9090
    volumes:
      - /Users/jake_kakao/workspace/prom-test/prometheus.yml:/etc/prometheus/prometheus.yml

  grafana:
    image: grafana/grafana
    ports:
      - 3000:3000
    depends_on:
      - prometheus


